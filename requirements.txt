# Install: pip install -r requirements.txt

# Producer: publish Avro events to Azure Event Hub
confluent-kafka>=2.3.0
fastavro>=1.9.0

# Spark: structured streaming consumer + analytics
pyspark>=4.0.0

# Dashboard: real-time visualization
streamlit>=1.30.0
pandas>=2.0.0

# Parquet I/O for the dashboard
pyarrow>=14.0.0

# Optional: only needed when reading from Azure Blob / ADLS in the dashboard
# (Spark handles Azure writes itself via the hadoop-azure JAR)
adlfs>=2024.2.0
